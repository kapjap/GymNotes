<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_background"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    tools:context=".Sign_Up_Activity">

    <TextView
        android:id="@+id/back_button2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginTop="32dp"
        android:fontFamily="@font/roboto"
        android:text="@string/back"
        android:textColor="@color/primary"
        android:textSize="16sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="36dp"
        android:layout_marginTop="20dp"
        android:fontFamily="@font/roboto_bold"
        android:text="@string/create_account"
        android:textColor="@color/app_text_primary"
        android:textSize="34sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/back_button2" />

    <EditText
        android:id="@+id/register_input2"
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:layout_marginTop="32dp"
        android:background="@drawable/bg_input"
        android:fontFamily="@font/roboto"
        android:hint="@string/email"
        android:inputType="textEmailAddress"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:textColor="@color/app_text_primary"
        android:textColorHint="@color/app_text_secondary"
        android:textSize="16sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/title" />

    <EditText
        android:id="@+id/register_input"
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:layout_marginTop="16dp"
        android:background="@drawable/bg_input"
        android:fontFamily="@font/roboto"
        android:hint="@string/password"
        android:inputType="textPassword"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:textColor="@color/app_text_primary"
        android:textColorHint="@color/app_text_secondary"
        android:textSize="16sp"
        app:layout_constraintEnd_toEndOf="@id/register_input2"
        app:layout_constraintStart_toStartOf="@id/register_input2"
        app:layout_constraintTop_toBottomOf="@id/register_input2" />

    <EditText
        android:id="@+id/name_input"
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:layout_marginTop="16dp"
        android:background="@drawable/bg_input"
        android:fontFamily="@font/roboto"
        android:hint="@string/first_name"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:textColor="@color/app_text_primary"
        android:textColorHint="@color/app_text_secondary"
        android:textSize="16sp"
        app:layout_constraintEnd_toEndOf="@id/register_input"
        app:layout_constraintStart_toStartOf="@id/register_input"
        app:layout_constraintTop_toBottomOf="@id/register_input" />

    <EditText
        android:id="@+id/lastName_input"
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:layout_marginTop="16dp"
        android:background="@drawable/bg_input"
        android:fontFamily="@font/roboto"
        android:hint="@string/last_name"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:textColor="@color/app_text_primary"
        android:textColorHint="@color/app_text_secondary"
        android:textSize="16sp"
        app:layout_constraintEnd_toEndOf="@id/name_input"
        app:layout_constraintStart_toStartOf="@id/name_input"
        app:layout_constraintTop_toBottomOf="@id/name_input" />

    <Button
        android:id="@+id/register_button"
        style="@style/Widget.Material3.Button"
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:layout_marginTop="28dp"
        android:backgroundTint="@color/primary"
        android:fontFamily="@font/roboto"
        android:padding="12dp"
        android:text="@string/create_account"
        android:textAllCaps="false"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        app:cornerRadius="10dp"
        app:layout_constraintEnd_toEndOf="@id/lastName_input"
        app:layout_constraintStart_toStartOf="@id/lastName_input"
        app:layout_constraintTop_toBottomOf="@id/lastName_input" />

</androidx.constraintlayout.widget.ConstraintLayout>